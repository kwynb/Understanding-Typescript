function add(num1: number, num2: number, showResult: boolean) {
    if (showResult)
        console.log(num1 + num2)   
    return num1 + num2;
}



const first = 5;
const second = 2.8;
const prints = true;

add(first, second, prints);
